# Steady site

Support page and privacy policy for the Steady: Symptom Tracker iOS app, served with GitHub Pages.

The app links here from Setup; the URLs are in `src/constants/app.ts` of the app repo.
Keep the two in step: `PRIVACY_URL` must resolve, or App Store review stops on it.
